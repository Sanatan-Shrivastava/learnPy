"""
list are containers in which we store, manipulate, or read data from.
These are ordered, and changaeble.
These allow duplicates members/elements.

represented by sqaure brackets name_of_list = []
"""




# s[0] -> represents the element at the location '0' (string, list, tuple)
# s = [] -> creates an empty list;

# Operations:

'''
[*] Declaration:
name = [] -> creates an empty list

[*] To find the length of the list. use len(<list_name>) in-built function:
'''
# to find the current length of the list: len(<list_name>);
my_list = []
print("The current size is :" + len(my_list))